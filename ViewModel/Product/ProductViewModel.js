import { GetProductList } from "../../Redux/Action/ProductAction";

const ProductListViewModel = 
{
    // const dispatch = 
    GetProductListById : (id, limit, page) => GetProductList (id, limit, page)
    
}
export default ProductListViewModel